# Recovery Evidence

Index recovery build, flash, boot failure, rollback, and forensic reports here.

Current local evidence source:

```text
<local-build-root>/repositories/rm10pro-orangefox-recovery/docs
```

The remote fork metadata was renamed to `Fractal-Echo/rm11pro-orangefox-recovery`; the local working directory still uses its original folder name.
