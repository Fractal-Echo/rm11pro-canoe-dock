# Recovery Patches

Patch sets for the local OrangeFox source tree live here.

Current lane:

- `fox_14.1/`: patches used by the RM11 Pro OrangeFox 14.1 recovery work.

These patches are local-build inputs. Public GitHub Actions may check that the
files exist, but must not apply them to a full Android checkout or run a full
build.
